[pytest]
addopts = -v --tb=short
testpaths = tests
python_files = test_*.py
python_functions = test_*
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    db: тести, що звертаються напряму до БД (потребують psycopg2 + credentials)
    async_test: async-варіант (httpx.AsyncClient)
    remote: тести проти зовнішнього API (restful-booker) — потребують мережі
